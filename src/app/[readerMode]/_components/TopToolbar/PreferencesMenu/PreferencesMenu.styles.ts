import { styled } from 'styled-system/jsx'
import { macrogrid } from 'styled-system/patterns'

export const PreferencesList = styled('form', {
	base: macrogrid.raw({ column: 'fullbleed', py: '6' }),
})
